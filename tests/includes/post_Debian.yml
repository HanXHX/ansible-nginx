---

- name: APT | Install webapps
  apt:
    pkg: "{{ packages }}"
    state: present
    install_recommends: no
  vars:
    packages:
      - backuppc

- name: SERVICE | Ensure backuppc is started
  service:
    name: backuppc
    state: started
